{
  "build": {
    "dockerfile": "Dockerfile",
    "dev": {
      "buildCommand": "npm install",
      "startCommand": "npm start"
    }
  },
  "deploy": {
    "onNewDeploy": {
      "buildCommand": "npm install",
      "startCommand": "npm start"
    }
  },
  "env": {
    "PORT": "3000"
  }
}
